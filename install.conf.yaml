- link:
    ~/.bashrc: bashrc
    ~/.gitconfig: gitconfig
    ~/.gitconfig.me: gitconfig.me
    ~/.tmux.conf: tmux.conf
    ~/.vimrc: vimrc

    ~/.pip/pip.conf:
      create: true
      path: pip.conf

    # curl -fLo ~/.vim/autoload/plug.vim --create-dirs https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim
    ~/.vim/autoload/plug.vim:
      create: true
      path: plug.vim
